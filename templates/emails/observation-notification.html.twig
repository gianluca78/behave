Dear user,
I just want to remind you about the observation of the {{ student.studentId }}'s behaviour "{{ behaviour }}" assigned to
you for {{ observationStartDate | date('l F j') }} from {{ observationStartDate | date('H:i') }} to {{ observationEndDate | date('H:i') }}.

Here you are the link to gather data: <a href="https://app.behaveproject.eu{{ path('measure', { 'id' : observation.id, 'token': observation.token }) }}">https://app.behaveproject.eu{{ path('measure', { 'id' : observation.id, 'token': observation.token }) }}</a> that will be enabled only
within the above mentioned time interval.




