{% extends 'base.html.twig' %}

{% block title %}

{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script src="{{ asset('js/duration_items_widget.js') }}"></script>
    <script src="{{ asset('js/frequency_items_widget.js') }}"></script>


    <script>
        $( '.square' ).on('click', function(e){
            $( "input[name*='xValue']" ).val($( this ).attr('data-row'));
            $( "input[name*='yValue']" ).val($( this ).attr('data-column'));
        });
    </script>
{% endblock %}