{% extends 'base.html.twig' %}

{% block title %}

{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <!--<script src="{{ asset('js/time-based-widgets.js') }}"></script>-->

    <script>
        var audioPath = '{{ asset('sounds/171756__nenadsimic__picked-coin-echo-2.wav') }}';
    </script>

    <script src="{{ asset('js/behavioral-recording-tools.js') }}"></script>

    <script>
        $( document ).ready(function() {
            $( '#form' ).behavioralRecordingTool(
                {
                    partialLengthInSeconds: 15
                }
            );
        });

        $( '.square' ).on('click', function(e){
            $( "input[name*='xValue']" ).val($( this ).attr('data-row'));
            $( "input[name*='yValue']" ).val($( this ).attr('data-column'));
        });
    </script>
{% endblock %}