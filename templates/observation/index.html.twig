{% extends 'base.html.twig' %}
{% block title %}
    {{ title }}
{% endblock %}

{% block body %}
    {% for flashMessage in app.session.flashbag.get('success') %}
        <div>{{ flashMessage }}</div>
    {% endfor %}


<table>
    <tr>
                <th>id</th>
                <th> name</th>
                <th></th>
        <th></th>
    </tr>
    {% for record in records %}
        <tr>
                        <td>{{ record.id }}</td>
                        <td>{{ record. name }}</td>
            
                        <td><a href="{{ path('observation_edit', {'id': record.id}) }}">{% trans %}Edit{% endtrans %}</a></td>
            <td><a href="{{ path('observation_delete', {'id': record.id}) }}">{% trans %}Delete{% endtrans %}</a></td>

                    </tr>
    
    {% endfor %}
    
</table>

<a href="{{ path('observation_new') }}">{% trans %}New{% endtrans %}</a>

{% endblock %}